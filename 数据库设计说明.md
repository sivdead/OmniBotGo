# OmniBotGo 数据库设计说明

## 🤖 项目定位

**OmniBotGo** 是一个Bot网关平台，提供统一的多平台消息接入和智能处理能力：

- **多Bot实例管理**: 支持创建多个独立功能的Bot
- **多平台连接**: 每个Bot可同时连接企业微信、钉钉、微信公众号等平台
- **智能消息路由**: 基于规则将消息分发给不同的处理器
- **代码驱动配置**: 平台和处理器类型在代码中定义，数据库存储实例配置

## 🏗️ 整体架构

### 三层设计模式

```
🤖 Bot管理层     : bots (Bot实例定义)
    ↓
🔌 通道接入层     : channels (平台通道配置)
    ↓
💬 消息处理层     : messages (统一消息模型) + message_queue (异步队列)
    ↓
🚀 智能路由层     : message_routing_rules + message_processors
    ↓
📊 监控审计层     : connection_logs + api_call_logs + system_configs
```

### 核心设计理念

1. **代码驱动**: 平台适配器和消息处理器的类型定义在代码中，确保类型安全
2. **配置分离**: 数据库只存储实例级别的配置参数，不包含元数据
3. **Bot中心化**: 所有资源围绕Bot实例组织，支持多Bot独立运行
4. **无状态中间件**: 不维护用户状态，专注消息转发和处理

## 📋 表结构设计

### 1. Bot管理层

#### bots (Bot实例表)
**功能**: 管理多个Bot实例，每个Bot有独立的功能定位和配置

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| bot_name | VARCHAR(100) | Bot名称，如"客服助手"、"AI小助手" |
| bot_type | VARCHAR(50) | Bot类型：standard-标准、ai-AI驱动、custom-自定义 |
| description | TEXT | Bot功能描述 |
| avatar_url | VARCHAR(500) | Bot头像URL |
| config | JSON | Bot特定配置，如工作时间、回复策略等 |
| status | TINYINT | 状态：0-禁用、1-启用、2-维护中 |
| created_by | VARCHAR(100) | 创建者 |

**索引设计**:
- `idx_bot_type` (bot_type)
- `idx_status` (status)

### 2. 通道接入层

#### channels (消息通道配置表)
**功能**: 配置Bot在各IM平台的连接通道

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| bot_id | BIGINT | 关联的Bot实例ID |
| platform_type | VARCHAR(50) | 平台类型，代码中定义：wecom、dingtalk、wechat_official |
| channel_name | VARCHAR(100) | 通道名称，如"生产环境-企业微信" |
| webhook_path | VARCHAR(200) | 唯一Webhook路径：/webhook/channel-{id} |
| config | JSON | 平台特定配置，代码校验格式 |
| access_token | VARCHAR(1000) | 运行时获取的访问令牌 |
| access_token_expires_at | TIMESTAMP | 令牌过期时间 |
| connection_status | TINYINT | 连接状态：0-未连接、1-已连接、2-连接失败 |
| last_connected_at | TIMESTAMP | 最后连接时间 |
| status | TINYINT | 通道状态：0-禁用、1-启用 |

**关键约束**:
- 外键: `bot_id` → `bots(id)`
- 唯一: `(bot_id, platform_type, channel_name)`

**索引设计**:
- `idx_bot_id` (bot_id)
- `idx_platform_type` (platform_type)
- `idx_webhook_path` (webhook_path) - 唯一索引

### 3. 消息处理层

#### messages (消息记录表)
**功能**: 统一消息模型，存储所有平台的消息

| 字段分组 | 字段 | 类型 | 说明 |
|----------|------|------|------|
| **标识** | message_id | VARCHAR(100) | 系统统一消息ID |
| | channel_id | BIGINT | 关联通道ID |
| | platform_message_id | VARCHAR(200) | 平台原始消息ID |
| **基础信息** | direction | TINYINT | 消息方向：1-接收、2-发送 |
| | message_type | VARCHAR(50) | 类型：text、image、audio、video、file |
| | content_type | VARCHAR(50) | MIME类型 |
| **发送者** | sender_id | VARCHAR(100) | 发送者平台ID |
| | sender_name | VARCHAR(100) | 发送者名称 |
| | sender_type | VARCHAR(50) | 类型：user、group、system |
| **接收者** | receiver_id | VARCHAR(100) | 接收者平台ID |
| | receiver_name | VARCHAR(100) | 接收者名称 |
| | receiver_type | VARCHAR(50) | 类型：user、group、system |
| **内容** | content | TEXT | 标准化文本内容 |
| | raw_content | JSON | 平台原始消息JSON |
| | unified_content | JSON | 统一格式消息 |
| **媒体** | media_url | VARCHAR(500) | 媒体文件URL |
| | media_type | VARCHAR(50) | 媒体类型 |
| | file_size | BIGINT | 文件大小 |
| **状态** | message_status | TINYINT | 1-已接收、2-处理中、3-已处理、4-发送中、5-已发送、6-失败 |
| | retry_count | INT | 重试次数 |
| | error_message | TEXT | 错误信息 |
| **关联** | parent_message_id | BIGINT | 回复的父消息ID |
| | conversation_id | VARCHAR(100) | 会话ID |
| | backend_request_id | VARCHAR(100) | 后端请求ID |
| **时间** | platform_timestamp | TIMESTAMP | 平台时间戳 |
| | received_at | TIMESTAMP | 接收时间 |
| | processed_at | TIMESTAMP | 处理完成时间 |
| | sent_at | TIMESTAMP | 发送时间 |

**关键索引**:
- `uk_message_id` (message_id) - 唯一
- `idx_channel_message` (channel_id, platform_message_id)
- `idx_conversation` (conversation_id)
- `idx_received_at` (received_at) - 时间范围查询

#### message_queue (消息队列表)
**功能**: 异步消息处理队列

| 字段 | 类型 | 说明 |
|------|------|------|
| queue_name | VARCHAR(100) | 队列名称 |
| message_id | VARCHAR(100) | 关联消息ID |
| payload | JSON | 队列消息载荷 |
| priority | INT | 优先级，数值越小优先级越高 |
| status | TINYINT | 0-待处理、1-处理中、2-已完成、3-失败 |
| scheduled_at | TIMESTAMP | 计划处理时间 |
| retry_count | INT | 重试次数 |
| max_retries | INT | 最大重试次数 |

### 4. 智能路由层

#### message_processors (消息处理器配置表)
**功能**: 配置消息处理器实例

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| processor_name | VARCHAR(100) | 处理器名称 |
| processor_type | VARCHAR(50) | 处理器类型，代码中定义 |
| config | JSON | 处理器实例配置，代码校验格式 |
| priority | INT | 处理器优先级 |
| status | TINYINT | 状态：0-禁用、1-启用 |

**处理器类型示例**:
- `openai_chat`: OpenAI对话处理器
- `webhook_forwarder`: Webhook转发器
- `order_query`: 订单查询处理器
- `email_notifier`: 邮件通知处理器

#### message_routing_rules (消息路由规则表)
**功能**: 定义消息如何路由到处理器

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| rule_name | VARCHAR(100) | 规则名称 |
| rule_description | TEXT | 规则描述 |
| bot_ids | JSON | 匹配的Bot ID列表 |
| platform_types | JSON | 匹配的平台类型列表 |
| channel_ids | JSON | 匹配的通道ID列表 |
| message_types | JSON | 匹配的消息类型列表 |
| sender_patterns | JSON | 发送者匹配模式 |
| content_patterns | JSON | 内容正则匹配模式 |
| processor_id | BIGINT | 目标处理器ID |
| route_type | TINYINT | 路由类型：1-同步、2-异步 |
| priority | INT | 规则优先级，数值越小优先级越高 |
| is_fallback | TINYINT | 是否为兜底规则 |
| condition_logic | VARCHAR(10) | 条件逻辑：AND、OR |

### 5. 监控审计层

#### connection_logs (连接状态日志表)
**功能**: 记录平台连接状态变化

| 字段 | 类型 | 说明 |
|------|------|------|
| channel_id | BIGINT | 关联通道ID |
| event_type | VARCHAR(50) | 事件类型：connect、disconnect、reconnect、error |
| status | TINYINT | 连接状态 |
| error_code | VARCHAR(50) | 错误代码 |
| error_message | TEXT | 错误信息 |
| details | JSON | 详细诊断信息 |

#### api_call_logs (API调用记录表)
**功能**: 记录所有API调用，支持性能监控

| 字段 | 类型 | 说明 |
|------|------|------|
| request_id | VARCHAR(100) | 请求ID |
| channel_id | BIGINT | 通道ID（平台API调用） |
| processor_id | BIGINT | 处理器ID（处理器API调用） |
| method | VARCHAR(10) | HTTP方法 |
| url | VARCHAR(1000) | 请求URL |
| request_headers | JSON | 请求头 |
| request_body | TEXT | 请求体 |
| response_status | INT | 响应状态码 |
| response_headers | JSON | 响应头 |
| response_body | TEXT | 响应体 |
| duration_ms | INT | 耗时（毫秒） |
| success | TINYINT | 是否成功 |

#### system_configs (系统配置表)
**功能**: 系统运行时配置

| 字段 | 类型 | 说明 |
|------|------|------|
| config_key | VARCHAR(100) | 配置键，唯一 |
| config_value | TEXT | 配置值 |
| config_type | VARCHAR(50) | 数据类型：string、int、bool、json |
| config_group | VARCHAR(50) | 配置分组 |
| description | TEXT | 配置说明 |
| is_encrypted | TINYINT | 是否加密存储 |
| is_system | TINYINT | 是否为系统配置 |

## 🔧 代码驱动设计

### 平台适配器注册
```go
// 平台元数据注册表
var PlatformRegistry = map[string]PlatformMetadata{
    "wecom": {
        Name:           "企业微信",
        APIBaseURL:     "https://qyapi.weixin.qq.com",
        RequiredFields: []string{"corp_id", "app_id", "app_secret", "agent_id"},
        OptionalFields: []string{"webhook_token", "webhook_secret"},
    },
    "dingtalk": {
        Name:           "钉钉",
        APIBaseURL:     "https://oapi.dingtalk.com",
        RequiredFields: []string{"app_id", "app_secret"},
        OptionalFields: []string{"webhook_token", "webhook_secret"},
    },
}
```

### 消息处理器注册
```go
// 处理器类型注册表
var ProcessorRegistry = map[string]ProcessorMetadata{
    "openai_chat": {
        Name:           "OpenAI对话处理器",
        RequiredFields: []string{"api_key", "model"},
        OptionalFields: []string{"temperature", "max_tokens", "base_url"},
        DefaultConfig:  map[string]interface{}{"temperature": 0.7},
    },
    "webhook_forwarder": {
        Name:           "Webhook转发器",
        RequiredFields: []string{"webhook_url"},
        OptionalFields: []string{"api_key", "timeout", "headers"},
    },
}
```

### 配置校验机制
```go
func ValidateChannelConfig(platformType string, config map[string]interface{}) error {
    metadata, exists := PlatformRegistry[platformType]
    if !exists {
        return fmt.Errorf("unsupported platform: %s", platformType)
    }
    
    for _, field := range metadata.RequiredFields {
        if _, exists := config[field]; !exists {
            return fmt.Errorf("missing required field: %s", field)
        }
    }
    return nil
}
```

## 🚀 开发实施指南

### 1. 初始化数据示例

```sql
-- 创建Bot实例
INSERT INTO bots (bot_name, bot_type, description, config) VALUES 
('智能客服', 'ai', '基于AI的智能客服机器人', '{"work_hours": "9:00-18:00", "auto_reply": true}'),
('订单助手', 'standard', '处理订单查询的专用Bot', '{"response_timeout": 30}');

-- 配置平台通道
INSERT INTO channels (bot_id, platform_type, channel_name, webhook_path, config) VALUES 
(1, 'wecom', '生产环境-企业微信', '/webhook/channel-1', '{
    "corp_id": "wwxxxxx",
    "app_id": "1000001",
    "app_secret": "xxxxx",
    "agent_id": "1000001"
}'),
(1, 'dingtalk', '生产环境-钉钉', '/webhook/channel-2', '{
    "app_id": "dingxxxxx", 
    "app_secret": "xxxxx"
}');

-- 创建消息处理器
INSERT INTO message_processors (processor_name, processor_type, config) VALUES 
('主AI处理器', 'openai_chat', '{
    "api_key": "sk-xxxxx",
    "model": "gpt-3.5-turbo",
    "temperature": 0.7
}'),
('订单查询API', 'order_query', '{
    "api_base_url": "https://order.company.com/api",
    "api_key": "order_api_key"
}');

-- 配置路由规则
INSERT INTO message_routing_rules (rule_name, bot_ids, content_patterns, processor_id, priority) VALUES
('订单查询路由', '[1]', '[".*订单.*", ".*查询.*"]', 2, 10),
('智能对话路由', '[1]', '[".*"]', 1, 100);
```

### 2. 消息处理流程

```go
func HandleWebhookMessage(channelPath string, rawMessage []byte) error {
    // 1. 根据webhook路径识别通道和Bot
    channel, err := FindChannelByWebhookPath(channelPath)
    if err != nil {
        return err
    }
    
    bot, err := GetBotByID(channel.BotID)
    if err != nil {
        return err
    }
    
    // 2. 解析平台消息
    platformAdapter := GetPlatformAdapter(channel.PlatformType)
    message, err := platformAdapter.ParseMessage(rawMessage, channel.Config)
    if err != nil {
        return err
    }
    
    // 3. 存储消息
    message.ChannelID = channel.ID
    err = SaveMessage(message)
    if err != nil {
        return err
    }
    
    // 4. 路由到处理器
    rules := FindMatchingRules(bot.ID, channel, message)
    for _, rule := range rules {
        processor := GetProcessor(rule.ProcessorID)
        response, err := processor.Process(message)
        if err != nil {
            LogError(err)
            continue
        }
        
        // 5. 发送回复
        if response != nil {
            err = platformAdapter.SendMessage(response, channel.Config)
            if err != nil {
                LogError(err)
            }
        }
        break // 只处理第一个匹配的规则
    }
    
    return nil
}
```

### 3. 数据库连接和配置

```go
type Config struct {
    Database struct {
        Host     string `yaml:"host"`
        Port     int    `yaml:"port"`
        Username string `yaml:"username"`
        Password string `yaml:"password"`
        Database string `yaml:"database"`
    } `yaml:"database"`
}

func InitDatabase(config *Config) (*sql.DB, error) {
    dsn := fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=utf8mb4&parseTime=True&loc=Local",
        config.Database.Username,
        config.Database.Password,
        config.Database.Host,
        config.Database.Port,
        config.Database.Database,
    )
    
    db, err := sql.Open("mysql", dsn)
    if err != nil {
        return nil, err
    }
    
    // 设置连接池参数
    db.SetMaxOpenConns(100)
    db.SetMaxIdleConns(10)
    db.SetConnMaxLifetime(time.Hour)
    
    return db, nil
}
```

### 4. 监控和指标

```go
type Metrics struct {
    MessageReceived    prometheus.Counter
    MessageProcessed   prometheus.Counter
    MessageFailed     prometheus.Counter
    ProcessingDuration prometheus.Histogram
    ConnectionStatus   prometheus.Gauge
}

func InitMetrics() *Metrics {
    return &Metrics{
        MessageReceived: prometheus.NewCounter(prometheus.CounterOpts{
            Name: "omnibot_messages_received_total",
            Help: "Total number of messages received",
        }),
        MessageProcessed: prometheus.NewCounter(prometheus.CounterOpts{
            Name: "omnibot_messages_processed_total", 
            Help: "Total number of messages processed successfully",
        }),
        ProcessingDuration: prometheus.NewHistogram(prometheus.HistogramOpts{
            Name: "omnibot_message_processing_duration_seconds",
            Help: "Time spent processing messages",
        }),
    }
}
```

## 📊 性能优化建议

### 1. 数据库优化
- **分表策略**: 消息表按月分表，提高查询性能
- **索引优化**: 根据查询模式创建复合索引
- **连接池**: 合理配置数据库连接池大小
- **缓存策略**: 热点配置数据使用Redis缓存

### 2. 消息处理优化
- **异步处理**: 非关键消息使用队列异步处理
- **批量操作**: 批量插入和更新减少数据库压力
- **限流控制**: 实现消息处理速率限制
- **超时控制**: 设置合理的处理超时时间

### 3. 监控告警
- **健康检查**: 实现完整的健康检查端点
- **性能指标**: 监控消息处理延迟和吞吐量
- **错误告警**: 关键错误实时告警
- **资源监控**: CPU、内存、数据库连接数监控

## 🔒 安全考虑

### 1. 敏感信息保护
- **配置加密**: API密钥等敏感配置加密存储
- **访问控制**: 实现基于角色的访问控制
- **审计日志**: 记录所有敏感操作
- **定期轮换**: 定期更新API密钥和访问令牌

### 2. 网络安全
- **HTTPS**: 所有外部通信使用HTTPS
- **签名验证**: 验证Webhook消息签名
- **IP白名单**: 限制API访问来源
- **速率限制**: 防止API滥用

这个设计文档现在提供了完整的开发指导，包含具体的代码示例、配置方法、性能优化和安全建议，可以直接指导项目的实际开发工作。 