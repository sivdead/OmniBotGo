---
description:
globs:
alwaysApply: true
---
# Go编码标准

## 代码规范

### 命名约定
- 包名：全小写，简短且有意义
- 变量名：驼峰式命名，私有变量以小写字母开头
- 函数名：驼峰式命名，公开函数以大写字母开头
- 常量：全大写，使用下划线分隔
- 接口名：通常以 -er 结尾（如 Repository, Handler）

### 错误处理
- 使用 `errors.New()` 或 `fmt.Errorf()` 创建错误
- 错误应该包含足够的上下文信息
- 在合适的层级记录错误日志
- 使用 `errors.Is()` 和 `errors.As()` 进行错误类型判断

### 结构体设计
- 将相关字段组织在一起
- 使用构造函数模式初始化复杂结构体
- 为结构体添加验证方法
- 使用嵌入来实现继承

### 接口设计
- 保持接口小而专一（单一职责）
- 在使用方定义接口，而不是实现方
- 使用接口进行解耦和测试

### 并发编程
- 使用 goroutine 进行并发处理
- 使用 channel 进行通信
- 避免共享状态，使用消息传递
- 使用 context 进行超时控制和取消

### 依赖管理
- 使用 Wire 进行依赖注入
- Provider函数应该简单明确
- 避免循环依赖
- 使用接口降低耦合度

### 测试编写
- 单元测试文件以 `_test.go` 结尾
- 测试函数以 `Test` 开头
- 使用 testify/assert 进行断言
- 使用 mock 进行依赖隔离

### 性能优化
- 避免不必要的内存分配
- 使用对象池复用大对象
- 合理使用 sync.Pool
- 进行性能基准测试
