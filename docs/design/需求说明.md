# **OmniBotGo 项目需求功能清单**

**项目目标**: 构建一个使用 Go 语言开发的、高性能、可扩展的统一消息适配器网关，能够**单一实例同时**连接和管理多个即时通讯平台（如企业微信、钉钉、公众号等），并将消息中继到后端业务逻辑或 AI 服务，实现消息的双向传递。

### **一、 核心引擎 (Core Engine) \- P0**

* **\[P0\] 多平台并发管理**:  
  * 支持在单个运行实例中，同时启动、管理和维护到多个不同 IM 平台的连接。  
  * 为每个平台连接实现独立的生命周期管理（启动、停止、重连）。  
  * 具备高并发处理能力，能稳定处理大量并发消息。  
* **\[P0\] 统一消息模型 (Unified Message Model)**:  
  * 定义一套内部标准的、与平台无关的消息格式。  
  * 实现将各平台接收的消息解析/转换为统一消息模型。  
  * 实现将统一消息模型转换为各平台发送所需的格式。  
* **\[P0\] 消息路由与分发**:  
  * 根据消息来源（平台、用户、群组等）将接收到的消息准确路由到后端服务。  
  * 根据后端指令将响应消息准确路由到目标平台及用户/群组。  
* **\[P0\] 后端服务对接**:  
  * 支持通过 HTTP/S Webhook 方式将消息推送到后端服务。  
  * 支持接收后端服务通过 API 推送过来的下行消息。  
  * 提供与后端服务对接的认证机制（如 Signature/Token）。  
* **\[P1\] 优雅启停与重连**:  
  * 支持服务的平滑启动和优雅停止，确保消息尽可能不丢失。  
  * 对断开的平台连接实现自动、可配置的重连机制（含退避策略）。

### **二、 平台适配器 (Platform Adapters)**

* **\[P0\] 企业微信 (WeCom) 适配器**:  
  * 支持企业微信**应用消息**的接收与发送。  
  * 支持 Webhook 方式接收消息（含加解密）。  
  * 支持发送文本、图片、Markdown、图文、文件、卡片等消息类型。  
  * 支持接收文本、图片、语音、视频、位置、事件（用户关注/取消、菜单点击等）消息。  
  * 处理 Access Token 的获取与刷新。  
* **\[P0\] 钉钉 (DingTalk) 适配器**:  
  * 支持钉钉**机器人**消息的接收与发送。  
  * 支持通过 Webhook 接收消息（含签名校验）。  
  * 支持发送文本、Markdown、链接、卡片（ActionCard, FeedCard）等消息类型。  
  * 支持接收文本、图片、语音等消息及 @ 机器人事件。  
  * (P1) 支持钉钉**企业内部应用**的消息收发（如果需要）。  
* **\[P0\] 微信公众号 (Official Account) 适配器**:  
  * 支持通过 Webhook 接收消息（XML 格式，含加解密）。  
  * 支持发送客服消息（JSON 格式）。  
  * 支持接收文本、图片、语音、视频、位置、链接、事件（关注/取消、扫码、菜单点击等）消息。  
  * 支持发送文本、图片、语音、视频、音乐、图文等消息类型。  
  * 处理 Access Token 的获取与刷新。  
* **\[P1\] 通用适配器能力**:  
  * 处理各平台 API 的速率限制 (Rate Limiting)。  
  * 提供清晰的平台 API 错误处理和日志记录。  
* **\[P2\] 更多平台支持**:  
  * 预留接口，方便未来扩展支持飞书、Telegram、Slack 等其他平台。

### **三、 消息处理 (Message Handling)**

* **\[P0\] 基本消息类型支持**: 文本 (Text)、图片 (Image)。  
* **\[P1\] 丰富消息类型支持**: Markdown、链接 (Link)、文件 (File)、语音 (Audio)、视频 (Video)、卡片 (Card \- 视平台支持情况)。  
* **\[P1\] 事件消息支持**: 关注/取消关注、进群/退群、菜单点击等平台特定事件。  
* **\[P1\] 上下文传递**: 能够在统一消息模型中携带必要的上下文信息（如 Platform ID, User ID, Group ID, Message ID, Timestamp 等）。  
* **\[P2\] 消息去重**: 提供可选的消息去重机制。  
* **\[P2\] 消息暂存/队列**: 在后端服务不可用时，提供可选的消息暂存或入队能力。

### **四、 配置与管理 (Configuration & Management)**

* **\[P0\] 统一配置文件**:  
  * 支持通过 YAML 或 TOML 格式的单一配置文件进行所有设置。  
  * 支持配置多个平台账号及其认证信息。  
  * 支持配置后端服务地址及认证信息。  
  * 支持配置日志级别、端口等基础设置。  
* **\[P1\] 环境变量覆盖**: 支持使用环境变量覆盖配置文件中的部分设置，方便 Docker/K8s 部署。  
* **\[P1\] 健康检查 API**: 提供 HTTP 健康检查端点 (/healthz)，方便监控系统集成。  
* **\[P2\] 状态监控 API**: 提供 API 以查询当前连接的平台状态、基本统计信息等。  
* **\[P2\] 热加载配置**: 支持不重启服务动态加载部分配置变更（需谨慎设计）。

### **五、 可扩展性与可维护性 (Extensibility & Maintainability)**

* **\[P1\] 模块化设计**: 代码结构清晰，各平台适配器、核心引擎、后端对接等模块解耦。  
* **\[P1\] 清晰的日志记录**:  
  * 使用结构化日志 (Structured Logging)，如 Zap 或 Logrus。  
  * 提供可配置的日志级别（Debug, Info, Warn, Error）。  
  * 关键操作和错误信息必须有日志记录。  
* **\[P1\] 完善的错误处理**: 对网络错误、API 错误、配置错误等进行捕获和处理。  
* **\[P2\] 插件/中间件机制**:  
  * 预留接口，允许用户通过插件或中间件扩展新平台或自定义消息处理流程。  
* **\[P1\] 单元测试与集成测试**: 关键模块提供单元测试覆盖，核心流程提供集成测试。  
* **\[P0\] 详细的文档**:  
  * 提供清晰的 README.md，说明项目功能、如何编译、配置和部署。  
  * 提供详细的配置说明和 API 文档。  
  * 提供开发者文档，说明如何扩展新平台或插件。

### **六、 非功能性需求 (Non-Functional Requirements)**

* **\[P0\] 性能**: 高吞吐量，低延迟。能够高效处理并发消息。  
* **\[P0\] 稳定性**: 7x24 小时稳定运行，具备良好的错误恢复能力。  
* **\[P0\] 安全性**: 妥善处理密钥、Token 等敏感信息，防止泄露；处理好 Webhook 的安全校验。  
* **\[P1\] 资源占用**: 尽可能低的 CPU 和内存占用。  
* **\[P0\] 部署友好**: 易于通过 Docker 或直接编译部署。  
* **\[P0\] 许可证**: 采用商业友好开源协议（如 MIT 或 Apache 2.0）。